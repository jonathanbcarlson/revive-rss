name: Rust

on:
  schedule:
    - cron: "0 15 * * *"
env:
  CARGO_TERM_COLOR: always

jobs:
  update_MP:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Run
      run: cargo run
