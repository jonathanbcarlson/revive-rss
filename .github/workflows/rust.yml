name: Rust

on:
  schedule:
    - cron: "0 15 * * *"
  workflow_dispatch:
    inputs:
      MPIndex:
        description: 'Morning Paper index'
        required: true
env:
  CARGO_TERM_COLOR: always

jobs:
  update_MP:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Run
      run: cargo run
